import { products } from "../../../db/db";

const ProductsReducer = (state = products, action) => {
    switch (action.type) {
        default:
            return state
    }
};
export default ProductsReducer;
